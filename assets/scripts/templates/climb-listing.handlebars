 <!-- {{#each books as |book|}}
  {{log book}}
  <section data-id="{{book.id}}">
    <h6>hello</h6>
    <h3>
      {{limit book.title 15}}
    </h3>
    <h6>Author:</h6>
    <p>
      {{#if book.author}}
        {{book.author}}
      {{else}}
        No author provided.
      {{/if}}
    </p>
    {{> partial}}
    <button data-id="{{book.id}}" class="btn btn-danger remove-book">Remove book!</button>
  </section>

{{/each}} -->
